n = int(input()) # Дано натуральное число. Напишите программу, которая вычисляет:сумму его цифр; количество цифр в нем;произведение его цифр;среднее арифметическое его цифр;его первую цифру;сумму его первой и последней цифры.
sum = 0
c = 0 # количество
mult = 1
a = 1
k = 1
last_d = n % 10
while n != 0:
    last_n = n % 10
    sum = sum + last_n
    mult = mult * last_n
    c = c + 1
    a = sum / c
    k = last_d + last_n
    n = n // 10
print(sum, c, mult, a, last_n, k, sep = '\n')
